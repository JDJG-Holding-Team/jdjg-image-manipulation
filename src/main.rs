use std::io::Cursor;
use image::io::Reader as ImageReader;


fn main() {
    // https://docs.rs/image/latest/image/
}

fn crusty(){
    //idk about the type needed to be passed in
    // any error will return the og.
}
